Please create comprehensive documentation for this function following [JSDoc/Python docstring/etc.] conventions:

Python
from datetime import datetime

from models import TaskStatus, TaskPriority

def calculate_task_score(task):
    """Calculate a priority score for a task based on multiple factors."""
    # Base priority weights
    priority_weights = {
        TaskPriority.LOW: 1,
        TaskPriority.MEDIUM: 2,
        TaskPriority.HIGH: 4,
        TaskPriority.URGENT: 6
    }

    # Calculate base score from priority
    score = priority_weights.get(task.priority, 0) * 10

    # Add due date factor (higher score for tasks due sooner)
    if task.due_date:
        days_until_due = (task.due_date - datetime.now()).days
        if days_until_due < 0:  # Overdue tasks
            score += 35
        elif days_until_due == 0:  # Due today
            score += 20
        elif days_until_due <= 2:  # Due in next 2 days
            score += 15
        elif days_until_due <= 7:  # Due in next week
            score += 10

    # Reduce score for tasks that are completed or in review
    if task.status == TaskStatus.DONE:
        score -= 50
    elif task.status == TaskStatus.REVIEW:
        score -= 15

    # Boost score for tasks with certain tags
    if any(tag in ["blocker", "critical", "urgent"] for tag in task.tags):
        score += 8

    # Boost score for recently updated tasks
    days_since_update = (datetime.now() - task.updated_at).days
    if days_since_update < 1:
        score += 5

    return score

def sort_tasks_by_importance(tasks):
    """Sort tasks by calculated importance score (highest first)."""
    task_scores = [(calculate_task_score(task), task) for task in tasks]
    # Use key parameter to tell sorted() to only compare the scores (first element of tuple)
    sorted_tasks = [task for _, task in sorted(task_scores, key=lambda x: x[0], reverse=True)]
    return sorted_tasks

def get_top_priority_tasks(tasks, limit=5):
    """Return the top N priority tasks."""
    sorted_tasks = sort_tasks_by_importance(tasks)
    return sorted_tasks[:limit]

```

The documentation should include:
1. A clear description of what the function does
2. All parameters with types and descriptions
3. Return value with type and description
4. Any exceptions or errors that might be thrown
5. Example usage
6. Any important notes or edge cases developers should be aware of



AI Response
from datetime import datetime
from models import TaskStatus, TaskPriority

def calculate_task_score(task):
    """Calculate a priority score for a task based on multiple factors.

    Evaluates a task's priority, due date proximity, current status, metadata 
    tags, and recent update activity to compute a composite numerical score 
    representing its urgency and importance.

    :param task: An object representing the task with attributes:
                 ``priority``, ``due_date``, ``status``, ``tags``, and ``updated_at``.
    :type task: object
    :return: The calculated priority score. Higher scores indicate greater 
             urgency and importance (negative scores are possible).
    :rtype: int
    :raises AttributeError: If the provided `task` object lacks required attributes.
    
    :Example:

    >>> task = Task(
    ...     priority=TaskPriority.HIGH,
    ...     due_date=datetime(2026, 6, 10),
    ...     status=TaskStatus.TODO,
    ...     tags=["critical"],
    ...     updated_at=datetime.now()
    ... )
    >>> score = calculate_task_score(task)
    """
    # Base priority weights
    priority_weights = {
        TaskPriority.LOW: 1,
        TaskPriority.MEDIUM: 2,
        TaskPriority.HIGH: 4,
        TaskPriority.URGENT: 6
    }

    # Calculate base score from priority
    score = priority_weights.get(task.priority, 0) * 10

    # Add due date factor (higher score for tasks due sooner)
    if task.due_date:
        days_until_due = (task.due_date - datetime.now()).days
        if days_until_due < 0:  # Overdue tasks
            score += 35
        elif days_until_due == 0:  # Due today
            score += 20
        elif days_until_due <= 2:  # Due in next 2 days
            score += 15
        elif days_until_due <= 7:  # Due in next week
            score += 10

    # Reduce score for tasks that are completed or in review
    if task.status == TaskStatus.DONE:
        score -= 50
    elif task.status == TaskStatus.REVIEW:
        score -= 15

    # Boost score for tasks with certain tags
    if any(tag in ["blocker", "critical", "urgent"] for tag in task.tags):
        score += 8

    # Boost score for recently updated tasks
    days_since_update = (datetime.now() - task.updated_at).days
    if days_since_update < 1:
        score += 5

    return score

def sort_tasks_by_importance(tasks):
    """Sort tasks by calculated importance score (highest first).

    :param tasks: A collection of task objects to be sorted.
    :type tasks: list
    :return: A new list containing the tasks ordered from highest priority score to lowest.
    :rtype: list
    """
    task_scores = [(calculate_task_score(task), task) for task in tasks]
    sorted_tasks = [task for _, task in sorted(task_scores, key=lambda x: x[0], reverse=True)]
    return sorted_tasks

def get_top_priority_tasks(tasks, limit=5):
    """Return the top N priority tasks.

    :param tasks: A collection of task objects to evaluate.
    :type tasks: list
    :param limit: The maximum number of top tasks to return. Defaults to 5.
    :type limit: int, optional
    :return: A list containing up to ``limit`` highest-priority tasks.
    :rtype: list
    """
    sorted_tasks = sort_tasks_by_importance(tasks)
    return sorted_tasks[:limit]


Prompt 2: Intent and Logic Explanation
I need help documenting the intent and logic behind this code. Please:

from datetime import datetime

from models import TaskStatus, TaskPriority

def calculate_task_score(task):
    """Calculate a priority score for a task based on multiple factors."""
    # Base priority weights
    priority_weights = {
        TaskPriority.LOW: 1,
        TaskPriority.MEDIUM: 2,
        TaskPriority.HIGH: 4,
        TaskPriority.URGENT: 6
    }

    # Calculate base score from priority
    score = priority_weights.get(task.priority, 0) * 10

    # Add due date factor (higher score for tasks due sooner)
    if task.due_date:
        days_until_due = (task.due_date - datetime.now()).days
        if days_until_due < 0:  # Overdue tasks
            score += 35
        elif days_until_due == 0:  # Due today
            score += 20
        elif days_until_due <= 2:  # Due in next 2 days
            score += 15
        elif days_until_due <= 7:  # Due in next week
            score += 10

    # Reduce score for tasks that are completed or in review
    if task.status == TaskStatus.DONE:
        score -= 50
    elif task.status == TaskStatus.REVIEW:
        score -= 15

    # Boost score for tasks with certain tags
    if any(tag in ["blocker", "critical", "urgent"] for tag in task.tags):
        score += 8

    # Boost score for recently updated tasks
    days_since_update = (datetime.now() - task.updated_at).days
    if days_since_update < 1:
        score += 5

    return score

def sort_tasks_by_importance(tasks):
    """Sort tasks by calculated importance score (highest first)."""
    task_scores = [(calculate_task_score(task), task) for task in tasks]
    # Use key parameter to tell sorted() to only compare the scores (first element of tuple)
    sorted_tasks = [task for _, task in sorted(task_scores, key=lambda x: x[0], reverse=True)]
    return sorted_tasks

def get_top_priority_tasks(tasks, limit=5):
    """Return the top N priority tasks."""
    sorted_tasks = sort_tasks_by_importance(tasks)
    return sorted_tasks[:limit]

```


1. Explain what this code is trying to accomplish at a high level
2. Break down the logic step-by-step
3. Identify any assumptions or edge cases in the implementation
4. Suggest inline comments for complex parts
5. Note any potential improvements while maintaining the original functionality

Ai Response
Here is a comprehensive breakdown of the intent, logic, assumptions, and potential enhancements for the task prioritization module.

---

### 1. High-Level Intent

The module is designed to **automate task management triage**. Instead of relying purely on a static manual priority setting, it dynamically calculates a composite "importance score" for every task. By factoring in deadlines, workflow status, metadata tags, and recent activity, it surfaces the items that require immediate human attention.

---

### 2. Step-by-Step Logic Breakdown

* **Base Scoring (`calculate_task_score`):**
* **Priority Weights:** Maps enum values to baseline points (`LOW: 10`, `MEDIUM: 20`, `HIGH: 40`, `URGENT: 60`).
* **Deadline Proximity:** Adds extra weight depending on how close the deadline is—ranging from `+10` (due within a week) to a heavy `+35` bonus for overdue items.
* **Status Penalty:** Deprioritizes work that is winding down or finished (`REVIEW: -15`, `DONE: -50`).
* **Metadata Boost:** Rewards high-urgency indicators (`+8` for tags like `"blocker"`, `"critical"`, or `"urgent"`).
* **Momentum Boost:** Adds a small recency bonus (`+5`) if the task was modified within the last 24 hours.


* **Organization & Filtering (`sort_tasks_by_importance` & `get_top_priority_tasks`):**
* Computes scores once per task using a tuple list comprehension to avoid redundant function calls.
* Sorts descending using Python's built-in Timsort algorithm via a custom lambda key.
* Slices the sorted array to return only the top $N$ items (defaulting to 5).



---

### 3. Assumptions and Edge Cases

* **Attribute Availability:** Assumes the `task` object consistently exposes `.priority`, `.due_date`, `.status`, `.tags`, and `.updated_at`.
* **Timezone Consistency:** Uses naive `datetime.now()` calls. If `due_date` or `updated_at` are timezone-aware, Python will raise a `TypeError` during subtraction.
* **Tag Iteration:** Assumes `task.tags` is an iterable collection of strings. If `task.tags` is `None`, the `any(...)` comprehension will fail.

---

### 4. Suggested Inline Comments for Complex Parts

```python
def sort_tasks_by_importance(tasks):
    """Sort tasks by calculated importance score (highest first)."""
    # Pair each task with its score upfront to prevent recalculating inside sort()
    task_scores = [(calculate_task_score(task), task) for task in tasks]
    
    # Sort descending (reverse=True) using the score (index 0) as the sorting key
    sorted_tasks = [task for _, task in sorted(task_scores, key=lambda x: x[0], reverse=True)]
    return sorted_tasks

```

```python
    # Check if any high-urgency keywords exist in the task's tag collection
    if any(tag in ["blocker", "critical", "urgent"] for tag in task.tags):
        score += 8

```

---

### 5. Potential Improvements (Maintaining Original Behavior)

* **Add Type Hints:** Introducing type annotations makes the code safer and easier to maintain:
```python
from typing import List
def get_top_priority_tasks(tasks: List, limit: int = 5) -> List:

```


* **Safe Attribute Access:** Protect against missing attributes or `None` tags using `getattr()` or default values.
* **Configuration Constants:** Extract magic numbers (like `35`, `20`, `50`) into a configuration dictionary or class constants at the top of the file so product managers can tweak the scoring algorithm without touching the core logic.

Final Documentation version
from datetime import datetime
from typing import List, Optional, Any
from models import TaskStatus, TaskPriority

def calculate_task_score(task: Any) -> int:
    """Calculate a priority score for a task based on multiple factors.

    Evaluates a task's priority, due date proximity, current status, metadata 
    tags, and recent update activity to compute a composite numerical score 
    representing its overall urgency and importance.

    Args:
        task (Any): An object representing the task. Expected attributes include:
            - priority (TaskPriority): The base priority level.
            - due_date (datetime, optional): The task deadline.
            - status (TaskStatus): The current lifecycle state of the task.
            - tags (iterable of str, optional): Associated labels or keywords.
            - updated_at (datetime): Timestamp of the last modification.

    Returns:
        int: The calculated priority score. Higher scores indicate greater 
            urgency and importance. Negative scores are possible for completed tasks.

    Raises:
        AttributeError: If the provided `task` object lacks any required 
            attributes (`priority`, `status`, `tags`, `updated_at`).
        TypeError: If datetime comparisons encounter incompatible timezone-aware 
            and timezone-naive objects.

    Example:
        >>> task = Task(
        ...     priority=TaskPriority.HIGH,
        ...     due_date=datetime(2026, 6, 10),
        ...     status=TaskStatus.TODO,
        ...     tags=["critical"],
        ...     updated_at=datetime.now()
        ... )
        >>> score = calculate_task_score(task)
        >>> print(score)
        53

    Note:
        - Relies on system time via `datetime.now()` for deadline calculations.
        - Overdue tasks receive a significant boost (+35 points), while 
          completed tasks receive a severe penalty (-50 points).
    """
    # Base priority weights
    priority_weights = {
        TaskPriority.LOW: 1,
        TaskPriority.MEDIUM: 2,
        TaskPriority.HIGH: 4,
        TaskPriority.URGENT: 6
    }

    # Calculate base score from priority
    score = priority_weights.get(task.priority, 0) * 10

    # Add due date factor (higher score for tasks due sooner)
    if task.due_date:
        days_until_due = (task.due_date - datetime.now()).days
        if days_until_due < 0:  # Overdue tasks
            score += 35
        elif days_until_due == 0:  # Due today
            score += 20
        elif days_until_due <= 2:  # Due in next 2 days
            score += 15
        elif days_until_due <= 7:  # Due in next week
            score += 10

    # Reduce score for tasks that are completed or in review
    if task.status == TaskStatus.DONE:
        score -= 50
    elif task.status == TaskStatus.REVIEW:
        score -= 15

    # Boost score for tasks with certain tags (safely handle empty/None tags)
    if task.tags and any(tag in ["blocker", "critical", "urgent"] for tag in task.tags):
        score += 8

    # Boost score for recently updated tasks
    days_since_update = (datetime.now() - task.updated_at).days
    if days_since_update < 1:
        score += 5

    return score

def sort_tasks_by_importance(tasks: List[Any]) -> List[Any]:
    """Sort tasks by calculated importance score (highest first).

    Pre-computes scores for each task to avoid redundant execution inside the 
    sorting algorithm, then orders the collection in descending sequence.

    Args:
        tasks (List[Any]): A collection of task objects to be sorted.

    Returns:
        List[Any]: A new list containing the tasks ordered from highest priority 
            score to lowest priority score.

    Example:
        >>> sorted_backlog = sort_tasks_by_importance(my_tasks)
    """
    # Pair each task with its score upfront to prevent recalculating inside sort()
    task_scores = [(calculate_task_score(task), task) for task in tasks]
    
    # Sort descending (reverse=True) using the score (index 0) as the sorting key
    sorted_tasks = [task for _, task in sorted(task_scores, key=lambda x: x[0], reverse=True)]
    return sorted_tasks

def get_top_priority_tasks(tasks: List[Any], limit: int = 5) -> List[Any]:
    """Return the top N priority tasks.

    Evaluates and sorts an entire collection of tasks, slicing the result 
    to return only the top-tier items requiring immediate action.

    Args:
        tasks (List[Any]): A collection of task objects to evaluate.
        limit (int, optional): The maximum number of top tasks to return. 
            Defaults to 5.

    Returns:
        List[Any]: A list containing up to `limit` highest-priority tasks.

    Example:
        >>> immediate_focus = get_top_priority_tasks(my_tasks, limit=3)
    """
    sorted_tasks = sort_tasks_by_importance(tasks)
    return sorted_tasks[:limit]